# Git and parity-track reconciliation

## Repository facts

The reconciliation began on branch `work` at `0df0f5dd188fa9cb6657570cc78ecc21677e5b95` with a clean working tree. The merge base selected for the two available Workstream B commits is their public-page foundation, `f8280ea9f60dedb44d077bedf2659290d800254b`. `.git/config` contains no remotes, and no local or remote branches or tags exist beyond `work`; consequently there is no repository-normal authenticated fetch path.

| Reported commit                            | Object status                                     | Relationship/resolution                                                                                                   |
| ------------------------------------------ | ------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------- |
| `dc1ca2dd0f37274d1b2e5f64cbc7e5357fe544e9` | Absent (`git cat-file` and `git merge-base` fail) | Workstream A's prose claims are not treated as code evidence. No files or tests are invented from it.                     |
| `0934d48bc272655c549ac0d7d5dc16b3425b1ac8` | Present                                           | Ancestor of starting HEAD; introduced the global skip link, reduced indexing, and initial developer gates.                |
| `0df0f5dd188fa9cb6657570cc78ecc21677e5b95` | Present                                           | Starting HEAD; imported the exact snapshot, route reviews, developer review, screenshots, audit script, and parity tests. |

## Available file overlap and unique behavior

`0934d48b` changed `src/lib/developer-docs.ts`, `src/lib/seo-policy.mjs`, `$locale.home.tsx`, `$slug.tsx`, `__root.tsx`, `assistants.tsx`, and `developers.$docSlug.tsx`. `0df0f5dd` changed the source inventory, page-parity report, review manifests, audit script, parity tests, and four screenshots. The commits are sequential rather than competing file-level branches; their semantic conflict came from the later 64-route/15-developer decision, not an unresolved Git merge.

The available track uniquely supplies exact snapshot assertions, a reusable content audit, route-precedence assertions, four representative screenshots, and the visible-on-focus root skip link. Workstream A's claimed additional route files, 19 tests, 119 runtime checks, and 12 screenshots cannot be compared because neither its commit nor any ref/artifact containing those changes exists. The test inventory therefore retains every one of the 140 available `*.test.mjs` files and records the unavailable delta instead of fabricating a union.

## Chosen semantic resolution

- **Sitemap 24 versus 64:** rebuilt from `src/lib/seo-policy.mjs` and runtime-reviewed content. The final 23 retain substantive explicit KovaGPT pages that satisfy existing public-route contracts. Thin generic marketing registries, empty publication/assistant directories, unreviewed locales, generic article samples, and all developer previews remain noindex. `src/lib/seo-policy.mjs` is the sole sitemap source.
- **Developer 0 versus 15:** all 19 topics remain useful informational previews, but none is a published external API contract. Internal Supabase-session routes are not relabeled as a public developer API. All 19 are noindex pending versioned endpoints, schemas, credentials, billing terms, and SDK contracts.
- **Tests 295 versus 276:** the current executable union is 277 unit cases across 140 available test files. The claimed extra 19 cannot be classified without the absent commit. No current test was deleted, skipped, weakened, or quarantined.
- **Generated artifacts:** `src/routeTree.gen.ts` remains generated by TanStack and was not hand-merged. Screenshots are evidence, not production assets. `package-lock.json` remains the package source of truth and was untouched.

## Current authorities

The machine reconciliation manifest names exactly one route manifest, sitemap module, content-review manifest, developer contract report, source inventory, and final release report. Earlier `docs/page-parity/kova-route-manifest.json` and developer verification JSON now contain explicit supersession pointers rather than competing totals.
