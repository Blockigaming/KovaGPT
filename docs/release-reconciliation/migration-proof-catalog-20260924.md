# Migration object inventory — 2026-09-24

The 19 structural lineage candidates remain `requires_schema_proof`. This change records a
read-only live catalog snapshot and makes CI capture the same scope after a production-shaped
98-version replay and after the source replay. It does not approve a migration-history action,
production schema change, restore, deployment, or any of the 19 proofs.

The [live artifact](evidence/migration-proof-live-object-digests-20260924.json) contains the
asserted project reference `mfbycmbjygcfkrsuepxf`, PostgreSQL 17, 98 ordered ledger versions,
19 remote-only version/statement-array hashes, 124 relation IDs with separate structure,
privilege and RLS hashes, 79 routine IDs with definition/security hashes, two schema-level ACL
hashes and a default-ACL hash.
It also records two public enum/domain type definitions with effective type privileges.
It captures no customer records, SQL function bodies, or credentials. The collector runs in a
single `REPEATABLE READ READ ONLY` transaction with local timeouts. The source query's SHA-256
is stored in the live artifact; CI must use that exact query and upload the source baseline and
source-final result in `database-upgrade-evidence/upgrade-migration-proof-catalog.json`.
Relation digests include view definitions/options, index validity/readiness, trigger enablement,
effective `MAINTAIN` privileges and API-role RLS bypass attributes. Routine digests include
schema `USAGE` alongside execution grants.
They also cover relation persistence, effective column privileges, sequence settings,
publication membership and column collations.
The structure digest also covers internal constraint-trigger enablement, replica identity and
rewrite rules; policy digests include the effective API-role membership in policy roles.

The source capture is generated by `npm run release:db:upgrade` in a disposable local PostgreSQL
17 project on CI. Both source snapshots, the source commit/tree, the query digest, the expected
ordered ledgers, and the artifact digest in `upgrade-database.json` must be checked before
comparing. The 19 rows in the live capture are asserted as one statement each; a missing row or
malformed digest fails the collector. The SQL connection target and query identity are operator
assertions, not independently attested production identity.

The disposable Supabase CLI may record migration statement arrays differently from the hosted
ledger. The isolated parser requires all 19 ordered historical versions and well-formed hashes,
but does not assert single-statement equivalence for its locally generated ledger rows. The
live parser does require all 19 one-statement rows. Historical body equivalence is established
by the separately validated 98-row fixture manifest and live ledger comparison, not by comparing
local CLI statement-array hashes to production hashes.

For each mapping, compare the live state with the local 98-version baseline and source-final
state for all affected objects and dependent objects. Inspect every structural, ACL, RLS and
function difference, migration body and data transformation, including object creation/deletion,
runtime behavior and dynamic SQL. A matching hash is only evidence for the catalog fields the
collector covers. It does not validate customer-row transformations, out-of-scope schemas,
behavioral tests or future privilege defaults. An independent reviewer must accept a
version-specific proof bound to exact project/query/ledger/source and artifact digests before
any `schema_proven` promotion. Keep the separate canonical-history, verified restore, rollback,
staging and cutover gates closed until their acceptance evidence exists.
