-- Reviewed structural history fixture, never a live migration command.
-- Replayed only in the generated disposable local upgrade project.

drop function if exists public._kova_temp_export_day15(text);
;
