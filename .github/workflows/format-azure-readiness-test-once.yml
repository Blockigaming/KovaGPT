name: Format Azure readiness test once

on:
  push:
    branches:
      - fix/azure-container-readiness-signal
    paths:
      - .github/workflows/format-azure-readiness-test-once.yml

permissions:
  contents: write

jobs:
  format:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262
        with:
          ref: fix/azure-container-readiness-signal
          fetch-depth: 0
      - uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020
        with:
          node-version: 24
          cache: npm
      - run: npm ci --ignore-scripts --no-audit --no-fund
      - name: Format and verify the workflow contract
        run: |
          set -euo pipefail
          npx prettier --write tests/unit/azure-container-readiness-workflow.test.mjs
          npx prettier --check tests/unit/azure-container-readiness-workflow.test.mjs
          node --test tests/unit/azure-container-readiness-workflow.test.mjs
      - name: Remove this one-shot workflow
        run: rm .github/workflows/format-azure-readiness-test-once.yml
      - name: Commit deterministic formatting
        run: |
          set -euo pipefail
          git diff --check
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add --all
          git diff --cached --quiet && exit 0
          git commit -m "Format Azure readiness workflow test"
          git push origin HEAD:fix/azure-container-readiness-signal

# Existing-file retrigger for the one-shot workflow.
