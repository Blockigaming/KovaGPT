name: Search formatter oracle

on:
  pull_request:

permissions:
  contents: read

jobs:
  format:
    if: github.head_ref == 'codex/global-search-keyboard-20260904'
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm
      - run: npm ci --ignore-scripts --no-audit --no-fund
      - name: Print locked Prettier output
        shell: bash
        run: |
          npx prettier --write tests/integration/global-search-keyboard-source.test.mjs
          echo "FORMAT_ORACLE_BEGIN"
          base64 -w 0 tests/integration/global-search-keyboard-source.test.mjs
          echo
          echo "FORMAT_ORACLE_END"
