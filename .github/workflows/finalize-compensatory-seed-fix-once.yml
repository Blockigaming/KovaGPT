name: Finalize compensatory seed fix once

on:
  push:
    branches:
      - fix/fresh-db-compensatory-subscription-seed
    paths:
      - .github/workflows/finalize-compensatory-seed-fix-once.yml

permissions:
  contents: write

jobs:
  finalize:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repair branch
        uses: actions/checkout@v4
        with:
          ref: fix/fresh-db-compensatory-subscription-seed
          fetch-depth: 0

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm

      - name: Install locked dependencies
        run: npm ci --ignore-scripts --no-audit --no-fund

      - name: Format focused test
        run: npx prettier --write tests/unit/compensatory-subscription-migration.test.mjs

      - name: Run executable migration regression tests
        run: node --test tests/unit/compensatory-subscription-migration.test.mjs

      - name: Regenerate and verify migration manifest
        run: |
          set -euo pipefail
          npm run release:manifest
          npm run release:migrations

      - name: Remove this one-shot workflow
        run: rm .github/workflows/finalize-compensatory-seed-fix-once.yml

      - name: Commit deterministic outputs
        run: |
          set -euo pipefail
          git diff --check
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add --all
          git diff --cached --quiet && exit 0
          git commit -m "Finalize fresh-database compensation repair"
          git push origin HEAD:fix/fresh-db-compensatory-subscription-seed
